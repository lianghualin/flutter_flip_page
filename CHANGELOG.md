## 0.1.0

- Add `FlipPageController` for triggering flips from outside the widget
- Add `showButton` parameter to hide the built-in floating FLIP button
- Add `controller` parameter to `FlipPage` for external flip control
- Fully backward compatible — existing usage unchanged

## 0.0.1

- Initial release
- FlipPage widget with 3D Y-axis rotation animation
- Customizable front/back widgets and animation duration
- Built-in floating FLIP button
